import React, { useState, useEffect } from 'react';

import Nav from '../Nav/Nav';

const Home = (props) => {
    return (
        <div>
            <Nav />
            <h1>This is the homepage</h1>
        </div>
    );
};

export default Home;
