import React, { useState, useEffect } from 'react';

import Nav from '../Nav/Nav';
const Login = (props) => {
    return (
        <div>
            <Nav />
            <h1>Login here</h1>
        </div>
    );
};

export default Login;
